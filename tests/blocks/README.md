Tests for the [block parser](/docs/blocks.md) for the language.

Each test is an `.ast` file containing the input for the language and a
respective `.out` file describing its block structure.

List of tests:

- [basic lines](lines.ast)
